<p>こんにちは<%= @resource.email %>!</p>

<p>誰かがパスワード変更をリクエストしました。下のURLから変更ができます。</p>

<p><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>もし心当たりがない場合、このメールは無視してください。</p>
<p>上のURLから新しいパスワードを作らなければ、パスワードは変更されません。</p>
